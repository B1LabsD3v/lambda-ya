/**
 * EXAMPLE — shared DTOs / domain types.
 *
 * @example
 * ```ts
 * import type { ExamplePayload } from "@types_/example.example.types";
 * ```
 *
 * Excluded from `tsc` — reference only.
 */
export interface ExamplePayload {
  id: string;
}
